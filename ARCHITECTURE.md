# Архитектура проекта heat_sim

## Структура файлов

```
heat_sim/
├── main.c                  # Главная программа
├── heat.c                  # Вычислительные функции + свойства вещества
├── heat.h                  # Структуры данных и объявления
├── settings.h              # Параметры задачи
├── copper_properties.dat   # Табличные свойства меди
├── Makefile                # Сборка
├── ARCHITECTURE.md         # Этот файл
├── .gitignore
└── README.md
```

## Описание модулей

### settings.h — Параметры задачи

Все настройки моделирования в одном месте:

| Группа | Константы | Описание |
|--------|-----------|----------|
| Режим | `USE_SPLINE`, `DATA_FILE` | Выбор: табличные свойства (1) или постоянные (0) |
| Сетка | `NC`, `NN`, `NG`, `NT`, `NF`, `H` | 100 ячеек + 2 призрачные, 101 грань |
| Физика | `L`, `T_INIT`, `T1`, `T2` | Длина 1 м, начальная 300 К, ГУ: 400 К / 300 К |
| Время | `TAU`, `INTEGRATE_TIME` | Шаг 0.1 с, полное время 100 с |

### heat.h — Структуры данных и интерфейс

**Структура ячейки `cell_t`:**

| Поле | Тип | Описание |
|------|-----|----------|
| `rho` | double | Плотность ρ [кг/м³] |
| `lambda` | double | Теплопроводность λ [Вт/(м·К)] |
| `Cv` | double | Теплоёмкость Cv [Дж/(кг·К)] |
| `E` | double | Объёмная энергия E = ρ·Cv·T [Дж/м³] |
| `T` | double | Температура [К] |
| `x` | double | Координата центра ячейки [м] |
| `q` | double | Суммарный поток через грани [Вт/м²] |

**Структура грани `face_t`:**

| Поле | Тип | Описание |
|------|-----|----------|
| `cl`, `cr` | int | Индексы ячеек слева и справа |
| `T`, `lambda`, `q` | double | Интерполированные значения на грани |

### heat.c — Вычислительное ядро

Две части:

**1. Свойства вещества (переключаются через `USE_SPLINE`):**

| Функция | `USE_SPLINE=1` | `USE_SPLINE=0` |
|---------|----------------|----------------|
| `ReadProperties()` | Читает `copper_properties.dat` | Ничего не делает |
| `clambda(T)` | Интерполяция λ(T) | Возвращает 401 |
| `cCv(T)` | Интерполяция Cv(T) | Возвращает 386 |
| `crho(T)` | Интерполяция ρ(T) | Возвращает 8930 |
| `cqv(T)` | 0 (нет источников) | 0 |

`Interpolate()` — линейная интерполяция по табличным данным.

**2. Функции решателя:**

| Функция | Назначение |
|---------|-----------|
| `makeMesh(cells, faces, L)` | Создание равномерной сетки: координаты центров ячеек, связи граней |
| `init(cells, Tinit)` | Инициализация: T, E, ρ, λ, Cv во всех ячейках |
| `applyBoundaries(cells, T1, T2)` | Граничные условия 1 рода через призрачные ячейки [0] и [NC+1] |
| `computeFluxes(cells, faces)` | Вычисление потоков q на каждой грани (закон Фурье), разнос по ячейкам |
| `integrate(cells, τ)` | Шаг расщепления 1: E -= (τ/h)·q |
| `computeSources(cells, τ)` | Шаг расщепления 2: E += τ·qv |
| `updateTemp(cells)` | Пересчёт температуры: T = E/(ρ·Cv) |
| `updateParams(cells)` | Обновление свойств: ρ(T), λ(T), Cv(T) |

### main.c — Главная программа

Цикл моделирования:

```
ReadProperties()          ← чтение таблицы свойств
makeMesh()                ← создание сетки
init() + applyBoundaries()← начальные и граничные условия
┌─── Цикл по времени ────────────────┐
│  computeFluxes()    ← потоки (Фурье)│
│  integrate()        ← расщепление 1  │
│  computeSources()   ← расщепление 2  │
│  updateTemp()       ← T из E         │
│  updateParams()     ← λ(T), ρ(T)     │
│  applyBoundaries()  ← ГУ 1 рода      │
└─────────────────────────────────────┘
Вывод T(x) в stdout
```

### copper_properties.dat — Свойства меди

Табличные данные для интерполяции (4 столбца):

```
# T [K]   rho [кг/м³]   Cp [кДж/(кг·К)]   lambda [Вт/(м·К)]
```

Значения Cp в файле в кДж — при чтении конвертируются в Дж (×1000).

## Сборка

```bash
make            # gcc -Wall -Wextra -O2 main.c heat.c -o heat_sim -lm
make clean      # удалить heat_sim, *.o
```
