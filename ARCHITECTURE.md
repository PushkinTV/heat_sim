# Архитектура проекта heat_sim

## Постановка задачи

Связанная термоупругая задача для тонкой мембраны SiO₂:
- **Теплопроводность** по толщине z: уравнение Каттанео (гиперболическое), ГУ 2 рода (лазерный поток)
- **Прогиб** по длине x: уравнение Эйлера-Бернулли с тепловым моментом как нагрузкой
- **Газовый модуль** (постобработка): конвективный теплообмен воздуха с нижней стенкой

## Численная схема

Тепловая задача решается **методом конечных объёмов** на сетке типа «ячейка-грань». Область [0, H_THICK] разбивается на NC расчётных ячеек. Каждая ячейка хранит средние значения (T, E, ρ, λ, Cv), потоки — на гранях.

**Призрачные ячейки** `[0]` и `[NC+1]` реализуют ГУ 2 рода (лазерный поток q₀(t)):

```
 ghost        внутренние ячейки (1..NC)        ghost
 [0]    |  [1]  |  [2]  | ... | [NC]  |  [NC+1]
q₀→T₀  f0  f1    f2          fNC-1  fNC  T_{NC+1}←q₀
```

Тепловой поток на гранях — **уравнение Каттанео** (неявная схема):

```
q^{n+1} = (q^n - (τ/τ_c)·λ·ΔT/h) / (1 + τ/τ_c)
```

при τ >> τ_c вырождается в закон Фурье. Явная схема неустойчива (τ/τ_c ~ 10⁹).

**Метод расщепления Ричардсона** на каждом временном шаге:

1. Потоки (Каттанео): `E -= (τ/h) · Δq`
2. Источники: `E += τ · qv`
3. Температура: `T = E / (ρ·Cv)`
4. Свойства: `ρ(T)`, `λ(T)`, `Cv(T)`

**Адаптивный шаг** по диффузионному CFL: `τ = k·h²/(2·α_max)`

## Термоупругая связь

На каждом шаге из температурного поля вычисляются:

```
M = ∫ z·(-E·β·(T-T₀)) dz   — тепловой момент [Н/м]
D = ∫ z²·E(T) dz            — изгибная жёсткость [Н·м]
T_tens = T₀ - E·h·β·ΔT      — мембранное натяжение [Н/м]
```

Эквивалентная нагрузка в уравнении Эйлера-Бернулли: `q_th = T₀·M/D`

Прогиб решается **спектральным методом Галёркина** (базис — синусы Фурье на [0,1]).

## Структура файлов

```
heat_sim/
├── main.c              # Главная программа: цикл, связь тепло↔балка
├── heat.c              # Тепловой решатель + свойства SiO₂ + термоупругие функции
├── heat.h              # Структуры cell_t, face_t; объявления
├── static.c            # Решатель Эйлера-Бернулли (Галёркин)
├── static.h            # Объявления балочных функций
├── gas.c               # Конвективный теплообмен с газом (постобработка)
├── gas.h               # Объявление analyzeConvection()
├── settings.h          # Все параметры задачи
├── plot.py             # Визуализация (matplotlib)
├── Makefile            # Сборка
├── copper_properties.dat  # Табличные свойства (USE_SPLINE=1)
├── ARCHITECTURE.md
└── README.md
```

## Описание модулей

### settings.h

| Группа | Константы | Значение |
|--------|-----------|----------|
| Режим | `USE_SPLINE` | 0 — полиномы SiO₂, 1 — таблица |
| Сетка | `NC`, `NT`, `NF`, `H` | 20 ячеек по z, H=50 мкм |
| Геометрия | `H_THICK`, `L_BEAM` | 1 мм толщина, 1 мм длина |
| Температура | `T_INIT`, `T1`, `T2` | 573 K начальная |
| Лазер | `LASER_A`, `USE_LASER_FLUX` | q₀ = A·(eᵗ−1) |
| Каттанео | `C_PHONON` | 5500 м/с |
| CFL | `CFL_K`, `INTEGRATE_TIME` | k=0.5, t=20 с |
| Балка | `N_BEAM`, `L_BEAM`, `TENSION_0` | 101 узел, T₀=10 Н/м |
| Газ | `D_GAP` | 0.1 мм зазор |

### heat.c

**Свойства SiO₂ (полиномы, 300–1000 K):**

| Функция | Формула |
|---------|---------|
| `clambda(T)` | 1.38 − 6e-4·dT + 2e-7·dT² |
| `cCv(T)` | 745 + 0.5·dT − 2e-4·dT² |
| `crho(T)` | 2200 (константа) |
| `cbeta(T)` | 0.5e-6 (КТР) |
| `cE(T)` | 73e9 − 1e7·(T−300) (модуль Юнга) |

**Решатель:** `makeMesh`, `init`, `applyBoundaries`, `computeFluxes` (Каттанео), `integrate`, `computeSources`, `updateTemp`, `updateParams`

**Термоупругие функции:** `computeThermalMoment`, `computeBendingStiffness`, `averageTemperature`, `computeTension`

### static.c

Спектральный метод Галёркина для уравнения Эйлера-Бернулли. x нормирован на [0,1].

| Функция | Назначение |
|---------|-----------|
| `calculateStaticDeflection(u, tau)` | Статический прогиб (механическая нагрузка) |
| `performStep(wi, wim1, tau, eps, thermal_load)` | Один динамический шаг с тепловой нагрузкой |
| `fourier`, `phi`, `lambda` | Спектральные вспомогательные функции |

`eps = D/(L²·T)` — безразмерная изгибная жёсткость, пересчитывается на каждом шаге.

### gas.c

Постобработка после основного цикла. Не влияет на тепловой решатель.

Алгоритм: по известному q₀ и T_wall находит скорость воздуха u_e через итерацию Re↔Nu.
Свойства воздуха при 300 K: ν=1.57e-5, λ=0.026, Pr=0.71. Корреляции: Nu=4.36 (ламинар), Nu=0.023·Re⁰·⁸·Pr⁰·⁴ (Диттус-Белтер).

### main.c

```
calculateStaticDeflection()     ← механический прогиб (1 раз)
┌─── Цикл по времени ──────────────────────────┐
│  CFL → τ                                      │
│  computeFluxes() → integrate() →              │
│  computeSources() → updateTemp() →            │
│  updateParams() → applyBoundaries()           │
│  computeThermalMoment/BendingStiffness()      │
│  computeTension() → eps_t, thermal_load       │
│  performStep()    ← термоупругий шаг          │
└──────────────────────────────────────────────┘
Запись .dat → analyzeConvection()  ← газ (постобработка)
```

## Выходные файлы

| Файл | Содержимое |
|------|-----------|
| `T_profile.dat` | T(z) при t=20 с |
| `deflection_mech.dat` | Механический прогиб w_mech(x) |
| `deflection_thermo.dat` | Термоупругий прогиб w_thermo(x) |
| `deflection_compare.png` | Сравнение прогибов |
| `T_profile.png` | Профиль температуры |

## Сборка

```bash
make            # gcc -Wall -Wextra -O2 main.c heat.c static.c gas.c -o heat_sim -lm
make clean
python3 plot.py # графики
```
